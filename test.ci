module main;

# Cimple code

type record NameAge {
  var name string;
  var age int;
}

function f(a string, x int) {
  var m = 18;
  var n = 15;
  var z = m + n;
  # var z = bit_and(n, m);
  # z:print(std~err);
  # for var i = 0; true {
  #   var j = i;
  # }
}
