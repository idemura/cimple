* Identifier: typeName, entityName final. module only can be set if current values is null.
* Finals: struct fields, union variants and etc. Function parameters?
* Document mangling rules
* Lambdas: function (x, y) {}
* Function without body declaration (external): function c_func(s string);
* Imports: import, import type because of namespace.
* Exports (@export with parameter)
* For each loop
* Generics via named traits
* No enum in union C code generation
* Implement constant folding visitor
* Emit first forward decls and then types themselves.
* Use assertJ: use
* Annotations: cname
